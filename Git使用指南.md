# Git 使用指南

> 一份全面的 Git 版本控制系统学习手册，涵盖基础概念、常用命令及 GitHub 协作流程。

---

## 目录

1. [Git 简介](#git-简介)
2. [Git 的由来与历史](#git-的由来与历史)
3. [Git 的核心优势](#git-的核心优势)
4. [Git 安装与配置](#git-安装与配置)
5. [Git 核心概念](#git-核心概念)
6. [Git 基础命令](#git-基础命令)
7. [Git 分支管理](#git-分支管理)
8. [GitHub 协作指南](#github-协作指南)
9. [Git 高级技巧](#git-高级技巧)
10. [常见问题与解决方案](#常见问题与解决方案)
11. [最佳实践](#最佳实践)

---

## Git 简介

**Git** 是一个免费、开源的**分布式版本控制系统**（Distributed Version Control System, DVCS），用于高效地处理从小型到超大型项目的版本管理。

### 什么是版本控制？

版本控制是一种记录文件内容变化的系统，它允许你：
- 📜 查看文件的历史修改记录
- ⏪ 回退到任意历史版本
- 🔀 在不同版本之间进行比较
- 👥 多人协作开发同一项目

---

## Git 的由来与历史

### 诞生背景

Git 的诞生与 **Linux 内核**的开发密切相关：

| 时间 | 事件 |
|------|------|
| 1991-2002 | Linux 内核开发者通过补丁和归档文件传递代码变更 |
| 2002 | Linux 社区开始使用商业版本控制系统 **BitKeeper** |
| 2005年4月 | BitKeeper 与 Linux 社区合作破裂，收回免费使用权 |
| 2005年4月 | **Linus Torvalds** 开始开发 Git |
| 2005年6月 | Git 首次用于管理 Linux 内核代码 |

### Linus Torvalds 的设计目标

Linus Torvalds 基于使用 BitKeeper 的经验，为 Git 设定了以下目标：

1. **速度** - 必须非常快
2. **简洁的设计** - 核心功能简单直接
3. **强力支持非线性开发** - 支持数千个并行分支
4. **完全分布式** - 每个开发者都有完整的仓库副本
5. **能够高效管理超大规模项目** - 如 Linux 内核（数百万行代码）

### 名字由来

"Git" 在英式俚语中意为"讨厌的人"。Linus Torvalds 曾自嘲说：

> "I'm an egotistical bastard, and I name all my projects after myself. First 'Linux', now 'Git'."
>
> （我是个自负的混蛋，我所有项目都以自己命名。先是 'Linux'，现在是 'Git'。）

当然，Git 也可以理解为：
- **G**lobal **I**nformation **T**racker（全局信息追踪器）— 当它正常工作时
- 或者只是一个随机的三个字母组合

---

## Git 的核心优势

### 1. 🌐 分布式架构

```
┌─────────────────────────────────────────────────────────┐
│                    集中式 vs 分布式                      │
├────────────────────────┬────────────────────────────────┤
│      集中式 (SVN)      │         分布式 (Git)           │
├────────────────────────┼────────────────────────────────┤
│   中央服务器存储所有    │   每个开发者都有完整仓库副本    │
│   开发者只有工作副本    │   可以在本地进行所有操作        │
│   离线无法工作         │   离线也能提交、查看历史        │
│   中央服务器故障=灾难   │   任何副本都可以恢复整个项目    │
└────────────────────────┴────────────────────────────────┘
```

### 2. ⚡ 极致的速度

- **本地操作**：大多数操作在本地完成，无需网络
- **高效存储**：Git 使用快照（snapshot）而非差异（diff）存储
- **压缩优化**：自动压缩和打包对象

### 3. 🔒 数据完整性

Git 使用 **SHA-1 哈希**校验所有内容：

```
提交哈希示例: 24b9da6552252987aa493b52f8696cd6d3b00373
```

- 任何文件改动都会被检测
- 无法在 Git 不知情的情况下修改任何内容
- 保证历史记录的完整性和可靠性

### 4. 🌿 强大的分支管理

| 功能 | 说明 |
|------|------|
| 轻量级分支 | 创建分支只需要 41 字节（一个 SHA-1 值） |
| 快速切换 | 分支切换几乎瞬间完成 |
| 鼓励分支 | 支持频繁创建、合并分支 |
| 非线性开发 | 可以同时进行多条开发线 |

### 5. 📦 暂存区机制

Git 独特的**暂存区（Staging Area）**允许你：
- 精确控制哪些修改进入下次提交
- 分批提交不同的修改
- 在提交前预览变更

### 6. 🆓 免费开源

- 遵循 **GPL v2** 许可证
- 完全免费使用
- 活跃的社区支持

---

## Git 安装与配置

### Windows 安装

1. 访问 [Git 官网](https://git-scm.com/download/win)
2. 下载安装包并运行
3. 按照向导完成安装（推荐使用默认选项）

验证安装：
```bash
git --version
# 输出示例: git version 2.43.0.windows.1
```

### macOS 安装

```bash
# 方式1: 使用 Homebrew
brew install git

# 方式2: 下载官方安装包
# 访问 https://git-scm.com/download/mac
```

### Linux 安装

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install git

# CentOS/Fedora
sudo dnf install git

# Arch Linux
sudo pacman -S git
```

### 初始配置

安装完成后，需要进行基本配置：

```bash
# 设置用户名（必须）
git config --global user.name "Your Name"

# 设置邮箱（必须，与 GitHub 账户邮箱一致）
git config --global user.email "your.email@example.com"

# 设置默认分支名为 main（推荐）
git config --global init.defaultBranch main

# 设置默认编辑器（可选）
git config --global core.editor "code --wait"  # VS Code
git config --global core.editor "vim"          # Vim

# Windows 用户推荐设置
git config --global core.autocrlf true

# 查看所有配置
git config --list
```

### 配置层级

| 层级 | 命令参数 | 配置文件位置 | 作用范围 |
|------|---------|-------------|---------|
| 系统级 | `--system` | `/etc/gitconfig` | 所有用户 |
| 用户级 | `--global` | `~/.gitconfig` | 当前用户所有仓库 |
| 仓库级 | `--local` | `.git/config` | 当前仓库 |

---

## Git 核心概念

### 三种状态

Git 中的文件有三种状态：

```
┌──────────────────────────────────────────────────────────────┐
│                         Git 工作流程                          │
│                                                              │
│  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐    │
│  │  工作目录   │     │   暂存区    │     │  Git 仓库   │    │
│  │ (Working   │     │  (Staging   │     │   (Git      │    │
│  │ Directory) │     │    Area)    │     │ Directory)  │    │
│  └──────┬──────┘     └──────┬──────┘     └──────┬──────┘    │
│         │                   │                   │           │
│         │    git add        │                   │           │
│         │──────────────────>│                   │           │
│         │                   │    git commit     │           │
│         │                   │──────────────────>│           │
│         │                   │                   │           │
│         │             git checkout              │           │
│         │<──────────────────────────────────────│           │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

| 状态 | 英文 | 说明 |
|------|------|------|
| 已修改 | Modified | 文件已修改但未暂存 |
| 已暂存 | Staged | 修改已标记，将进入下次提交 |
| 已提交 | Committed | 数据已安全存入本地数据库 |

### 文件生命周期

```
┌─────────────────────────────────────────────────────────────┐
│                      文件状态变化图                          │
│                                                             │
│   Untracked    Unmodified     Modified      Staged         │
│       │            │              │            │            │
│       │   add      │              │            │            │
│       │───────────────────────────────────────>│            │
│       │            │              │            │            │
│       │            │    edit      │            │            │
│       │            │─────────────>│            │            │
│       │            │              │            │            │
│       │            │              │   stage    │            │
│       │            │              │───────────>│            │
│       │            │              │            │            │
│       │            │           commit          │            │
│       │            │<──────────────────────────│            │
│       │            │              │            │            │
│       │   remove   │              │            │            │
│       │<───────────│              │            │            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 关键术语

| 术语 | 说明 |
|------|------|
| **Repository (仓库)** | 包含项目所有文件和历史记录的目录 |
| **Commit (提交)** | 项目在某一时刻的快照 |
| **Branch (分支)** | 指向某个提交的可移动指针 |
| **HEAD** | 指向当前所在分支的指针 |
| **Remote (远程仓库)** | 托管在网络上的仓库，如 GitHub |
| **Clone (克隆)** | 复制远程仓库到本地 |
| **Pull (拉取)** | 从远程获取并合并更新 |
| **Push (推送)** | 将本地提交上传到远程 |
| **Merge (合并)** | 将一个分支的更改合并到另一个分支 |

---

## Git 基础命令

### 创建仓库

```bash
# 在当前目录初始化新仓库
git init

# 在指定目录初始化新仓库
git init <project-name>

# 克隆远程仓库
git clone <url>

# 克隆到指定目录
git clone <url> <directory>

# 克隆指定分支
git clone -b <branch-name> <url>
```

### 查看状态

```bash
# 查看工作区状态
git status

# 简洁模式
git status -s
# 输出示例:
#  M README.md      (M = 已修改)
# A  new-file.txt   (A = 已暂存)
# ?? untracked.txt  (?? = 未跟踪)
```

### 暂存文件

```bash
# 暂存指定文件
git add <file>

# 暂存多个文件
git add file1.txt file2.txt

# 暂存所有修改
git add .
git add -A

# 交互式暂存（选择性暂存代码块）
git add -p
```

### 提交更改

```bash
# 提交暂存的更改
git commit -m "提交信息"

# 添加详细说明（打开编辑器）
git commit

# 跳过暂存区，直接提交所有已跟踪文件的修改
git commit -a -m "提交信息"

# 修改最后一次提交
git commit --amend -m "新的提交信息"
```

### 提交信息规范

推荐使用 **Conventional Commits** 规范：

```
<type>(<scope>): <subject>

<body>

<footer>
```

常用类型：

| 类型 | 说明 |
|------|------|
| `feat` | 新功能 |
| `fix` | 修复 Bug |
| `docs` | 文档更新 |
| `style` | 代码格式（不影响功能） |
| `refactor` | 重构代码 |
| `test` | 测试相关 |
| `chore` | 构建/工具变更 |

示例：
```bash
git commit -m "feat(user): 添加用户登录功能"
git commit -m "fix(api): 修复接口超时问题"
git commit -m "docs(readme): 更新安装说明"
```

### 查看历史

```bash
# 查看提交历史
git log

# 单行显示
git log --oneline

# 显示分支图
git log --oneline --graph --all

# 显示最近 N 条
git log -n 5

# 显示详细改动
git log -p

# 按作者筛选
git log --author="name"

# 按时间筛选
git log --after="2024-01-01" --before="2024-12-31"

# 搜索提交信息
git log --grep="关键词"
```

### 查看差异

```bash
# 工作区与暂存区的差异
git diff

# 暂存区与最新提交的差异
git diff --staged
git diff --cached

# 两个提交之间的差异
git diff <commit1> <commit2>

# 两个分支之间的差异
git diff <branch1> <branch2>

# 查看某个文件的差异
git diff <file>
```

### 撤销操作

```bash
# 撤销工作区的修改（危险：会丢失修改）
git checkout -- <file>
git restore <file>  # Git 2.23+ 推荐

# 取消暂存
git reset HEAD <file>
git restore --staged <file>  # Git 2.23+ 推荐

# 撤销最后一次提交（保留修改）
git reset --soft HEAD~1

# 撤销最后一次提交（保留修改在工作区）
git reset --mixed HEAD~1  # 默认选项

# 撤销最后一次提交（完全丢弃修改，危险！）
git reset --hard HEAD~1

# 创建新提交来撤销之前的提交（安全）
git revert <commit>
```

> ⚠️ **警告**：`git reset --hard` 会永久删除未提交的更改，请谨慎使用！

### 删除文件

```bash
# 从 Git 和工作区同时删除
git rm <file>

# 仅从 Git 删除（保留本地文件）
git rm --cached <file>

# 强制删除（即使有修改）
git rm -f <file>
```

---

## Git 分支管理

### 分支基础操作

```bash
# 查看本地分支
git branch

# 查看所有分支（包括远程）
git branch -a

# 查看远程分支
git branch -r

# 创建新分支
git branch <branch-name>

# 切换分支
git checkout <branch-name>
git switch <branch-name>  # Git 2.23+ 推荐

# 创建并切换分支
git checkout -b <branch-name>
git switch -c <branch-name>  # Git 2.23+ 推荐

# 重命名分支
git branch -m <old-name> <new-name>

# 删除分支
git branch -d <branch-name>  # 安全删除（已合并）
git branch -D <branch-name>  # 强制删除
```

### 分支合并

```bash
# 合并指定分支到当前分支
git merge <branch-name>

# 合并时禁用快进（保留分支历史）
git merge --no-ff <branch-name>

# 合并时压缩提交
git merge --squash <branch-name>
```

### 合并冲突解决

当两个分支修改了同一文件的同一部分时，会产生冲突：

```
<<<<<<< HEAD
当前分支的内容
=======
要合并分支的内容
>>>>>>> branch-name
```

解决步骤：
1. 打开冲突文件，手动编辑
2. 删除冲突标记 (`<<<<<<<`, `=======`, `>>>>>>>`)
3. 保留需要的代码
4. 暂存并提交

```bash
# 解决冲突后
git add <file>
git commit -m "解决合并冲突"

# 放弃合并
git merge --abort
```

### 变基 (Rebase)

```bash
# 将当前分支变基到目标分支
git rebase <branch>

# 交互式变基（修改历史提交）
git rebase -i HEAD~3

# 变基冲突后
git rebase --continue  # 解决后继续
git rebase --abort     # 放弃变基
```

> 💡 **Merge vs Rebase**：
> - `merge` 保留完整的历史分支结构
> - `rebase` 创建更线性的历史，但会改写历史
> - **不要对已推送的提交进行 rebase！**

### 储藏 (Stash)

```bash
# 储藏当前修改
git stash
git stash save "描述信息"

# 查看储藏列表
git stash list

# 恢复储藏
git stash pop           # 恢复并删除
git stash apply         # 恢复但保留
git stash apply stash@{n}  # 恢复指定储藏

# 删除储藏
git stash drop stash@{n}
git stash clear         # 清空所有储藏
```

---

## GitHub 协作指南

### GitHub 简介

**GitHub** 是全球最大的代码托管平台，提供：
- 🗄️ Git 仓库托管
- 👥 团队协作工具
- 🔄 Pull Request 工作流
- 🤖 GitHub Actions (CI/CD)
- 📝 Issues 问题追踪
- 📖 Wiki 文档
- 📊 项目管理功能

### 创建 GitHub 账户

1. 访问 [github.com](https://github.com)
2. 点击 "Sign up"
3. 按照指引完成注册

### SSH 密钥配置

为了安全地与 GitHub 通信，推荐使用 SSH：

```bash
# 1. 生成 SSH 密钥
ssh-keygen -t ed25519 -C "your.email@example.com"
# 一路回车使用默认值

# 2. 启动 SSH 代理
eval "$(ssh-agent -s)"

# 3. 添加私钥到代理
ssh-add ~/.ssh/id_ed25519

# 4. 复制公钥
# Windows (Git Bash)
cat ~/.ssh/id_ed25519.pub | clip

# macOS
pbcopy < ~/.ssh/id_ed25519.pub

# Linux
xclip -sel clip < ~/.ssh/id_ed25519.pub

# 5. 添加到 GitHub
# 访问 GitHub -> Settings -> SSH and GPG keys -> New SSH key
# 粘贴公钥并保存

# 6. 测试连接
ssh -T git@github.com
# 成功: "Hi username! You've successfully authenticated..."
```

### 远程仓库操作

```bash
# 查看远程仓库
git remote -v

# 添加远程仓库
git remote add origin git@github.com:username/repo.git

# 修改远程仓库地址
git remote set-url origin <new-url>

# 删除远程仓库
git remote remove origin

# 重命名远程仓库
git remote rename origin upstream
```

### 推送与拉取

```bash
# 推送到远程
git push origin main

# 首次推送设置上游分支
git push -u origin main
# 之后可以直接使用
git push

# 强制推送（危险！会覆盖远程历史）
git push -f origin main

# 拉取远程更新并合并
git pull origin main
git pull  # 如果设置了上游分支

# 仅获取不合并
git fetch origin

# 拉取时使用变基
git pull --rebase origin main
```

### Pull Request 工作流

**Pull Request (PR)** 是 GitHub 协作的核心：

```
┌──────────────────────────────────────────────────────────────┐
│                    Pull Request 工作流                       │
│                                                              │
│  1. Fork 仓库                                                │
│     └─> 在 GitHub 上复制仓库到自己账户                        │
│                                                              │
│  2. Clone 到本地                                             │
│     └─> git clone git@github.com:你的用户名/repo.git         │
│                                                              │
│  3. 创建功能分支                                              │
│     └─> git checkout -b feature/new-feature                 │
│                                                              │
│  4. 开发并提交                                                │
│     └─> git add . && git commit -m "feat: 新功能"            │
│                                                              │
│  5. 推送到自己的仓库                                          │
│     └─> git push origin feature/new-feature                  │
│                                                              │
│  6. 在 GitHub 创建 Pull Request                              │
│     └─> 点击 "Compare & pull request"                        │
│                                                              │
│  7. 代码审查与讨论                                            │
│     └─> 回应评审意见，继续提交修改                             │
│                                                              │
│  8. 合并 PR                                                  │
│     └─> 维护者合并或关闭 PR                                   │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 保持 Fork 同步

```bash
# 添加上游仓库
git remote add upstream git@github.com:原作者/repo.git

# 获取上游更新
git fetch upstream

# 合并到本地 main
git checkout main
git merge upstream/main

# 推送到自己的仓库
git push origin main
```

### Issues 与项目管理

GitHub Issues 用于：
- 🐛 Bug 报告
- ✨ 功能请求
- 📋 任务追踪
- 💬 讨论

关键词自动关联：
```
提交信息中使用以下关键词可自动关闭 issue：
- Closes #123
- Fixes #123
- Resolves #123
```

### GitHub Actions (CI/CD)

在仓库中创建 `.github/workflows/ci.yml`：

```yaml
name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
    
    - name: Install dependencies
      run: npm install
    
    - name: Run tests
      run: npm test
```

---

## Git 高级技巧

### 交互式变基

```bash
# 修改最近 3 次提交
git rebase -i HEAD~3
```

编辑器中的命令：
- `pick` - 使用提交
- `reword` - 使用提交但修改提交信息
- `edit` - 使用提交但停下来修改
- `squash` - 使用提交但合并到前一个提交
- `fixup` - 类似 squash 但丢弃提交信息
- `drop` - 删除提交

### 二分查找 Bug

```bash
# 开始二分查找
git bisect start

# 标记当前为有 Bug
git bisect bad

# 标记某个已知正常的提交
git bisect good <commit>

# Git 会自动切换到中间提交
# 测试后标记
git bisect good  # 或
git bisect bad

# 重复直到找到问题提交
# 结束二分查找
git bisect reset
```

### Cherry Pick

```bash
# 将某个提交应用到当前分支
git cherry-pick <commit>

# 选择多个提交
git cherry-pick <commit1> <commit2>

# 选择一个范围
git cherry-pick <commit1>..<commit2>

# 不自动提交
git cherry-pick -n <commit>
```

### 子模块 (Submodule)

```bash
# 添加子模块
git submodule add <url> <path>

# 克隆包含子模块的仓库
git clone --recurse-submodules <url>

# 更新子模块
git submodule update --init --recursive

# 拉取子模块更新
git submodule update --remote
```

### 工作树 (Worktree)

```bash
# 添加工作树（同时在多个分支工作）
git worktree add ../feature-branch feature/my-feature

# 查看工作树
git worktree list

# 删除工作树
git worktree remove ../feature-branch
```

### Git Hooks

在 `.git/hooks/` 目录下创建脚本：

| Hook | 触发时机 |
|------|---------|
| `pre-commit` | 提交前 |
| `commit-msg` | 编辑提交信息后 |
| `pre-push` | 推送前 |
| `post-merge` | 合并后 |

示例 `pre-commit`：
```bash
#!/bin/sh
# 运行代码格式检查
npm run lint
if [ $? -ne 0 ]; then
  echo "代码格式检查失败，请修复后再提交"
  exit 1
fi
```

### .gitignore 文件

常用模板：

```gitignore
# 依赖目录
node_modules/
vendor/
venv/

# 构建输出
dist/
build/
*.exe
*.dll

# 编辑器配置
.idea/
.vscode/
*.swp

# 操作系统文件
.DS_Store
Thumbs.db

# 环境变量
.env
.env.local

# 日志
*.log
logs/

# 临时文件
*.tmp
*.temp

# 秘钥和凭证
*.pem
*.key
```

> 💡 访问 [gitignore.io](https://www.toptal.com/developers/gitignore) 生成适合你项目的 .gitignore

---

## 常见问题与解决方案

### Q1: 如何撤销已推送的提交？

```bash
# 方法1: 使用 revert（推荐，安全）
git revert <commit>
git push

# 方法2: 使用 reset + force push（危险）
git reset --hard HEAD~1
git push -f
# ⚠️ 不要在共享分支上使用！
```

### Q2: 如何修改已推送的提交信息？

```bash
# 修改最后一次
git commit --amend -m "新信息"
git push -f
# ⚠️ 谨慎使用力推送

# 修改更早的提交
git rebase -i HEAD~n
# 将要修改的提交改为 reword
```

### Q3: 如何找回误删的分支？

```bash
# 查看操作日志
git reflog

# 找到删除前的提交 SHA
git checkout -b recovered-branch <sha>
```

### Q4: 如何清理大文件历史？

```bash
# 使用 git-filter-repo（推荐）
pip install git-filter-repo
git filter-repo --strip-blobs-bigger-than 10M

# 或使用 BFG Repo-Cleaner
java -jar bfg.jar --strip-blobs-bigger-than 10M
```

### Q5: 如何处理换行符问题？

```bash
# Windows
git config --global core.autocrlf true

# macOS/Linux
git config --global core.autocrlf input

# 不转换
git config --global core.autocrlf false
```

### Q6: 如何加速 Git Clone？

```bash
# 浅克隆（只获取最近 N 次提交）
git clone --depth 1 <url>

# 单分支克隆
git clone --single-branch -b main <url>

# 稀疏检出（部分目录）
git clone --filter=blob:none --sparse <url>
cd repo
git sparse-checkout set path/to/dir
```

---

## 最佳实践

### ✅ 提交规范

1. **一次提交做一件事** - 每个提交应该是一个独立的逻辑单元
2. **写清晰的提交信息** - 说明做了什么和为什么
3. **提交前检查** - 运行测试，确保代码可用
4. **经常提交** - 小步快跑，不要积累大量修改

### ✅ 分支策略

1. **保护 main 分支** - 不直接在 main 提交
2. **使用功能分支** - 每个功能/修复一个分支
3. **定期同步** - 经常从 main 合并/变基
4. **删除已合并分支** - 保持分支列表整洁

### ✅ 协作规范

1. **先拉后推** - push 前先 pull 获取最新代码
2. **使用 Pull Request** - 代码审查后再合并
3. **不要力推共享分支** - 避免 `git push -f`
4. **及时解决冲突** - 不要让冲突积累

### ✅ 安全建议

1. **使用 SSH 或令牌** - 不要在 URL 中暴露密码
2. **不要提交敏感信息** - 使用 .gitignore 和 .env
3. **定期备份** - 重要仓库多处备份
4. **审查第三方代码** - fork 前检查代码

---

## 命令速查表

```bash
# 仓库
git init                    # 初始化仓库
git clone <url>             # 克隆仓库

# 基础操作
git status                  # 查看状态
git add <file>              # 暂存文件
git commit -m "msg"         # 提交
git log --oneline           # 查看历史

# 分支
git branch                  # 查看分支
git branch <name>           # 创建分支
git switch <name>           # 切换分支
git merge <name>            # 合并分支

# 远程
git remote -v               # 查看远程
git fetch                   # 获取更新
git pull                    # 拉取合并
git push                    # 推送

# 撤销
git restore <file>          # 撤销工作区修改
git restore --staged <file> # 取消暂存
git reset --soft HEAD~1     # 撤销提交（保留修改）
git revert <commit>         # 创建撤销提交

# 储藏
git stash                   # 储藏修改
git stash pop               # 恢复储藏
```

---

## 学习资源

### 官方资源
- 📚 [Pro Git 书籍](https://git-scm.com/book/zh/v2) - 官方免费电子书
- 📖 [Git 参考手册](https://git-scm.com/docs) - 命令详解

### 互动学习
- 🎮 [Learn Git Branching](https://learngitbranching.js.org/?locale=zh_CN) - 可视化学习
- 🏋️ [Git Exercises](https://gitexercises.fracz.com/) - 练习题

### 速查工具
- 🔍 [Git Explorer](https://gitexplorer.com/) - 根据需求查找命令
- 📋 [GitHub Cheat Sheet](https://training.github.com/downloads/zh_CN/github-git-cheat-sheet/)

---

> 📝 **文档版本**: 1.0  
> 📅 **最后更新**: 2026年1月26日  
> 👤 **作者**: AI Assistant

---

*Happy Coding with Git! 🚀*
